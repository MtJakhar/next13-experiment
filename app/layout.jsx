import Link from 'next/link'
import './globals.css'
import { Montserrat } from "next/font/google"

// you can import just about any font from next/font, and any google font from next/font/google
// declare a const variable and set that to your font type. Also to createa a tailwind css class just create a variable and name it what you want. and then add it to the tailwind config under theme.extends
const montserrat = Montserrat({
  weight:['400', '700'],
  subsets:['latin'],
  variable: '--font-montserrat',
})

export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

// What Does Layout do?
// Layout is a nextjs component that allows you to warp your pages with default reuseable components which are the same on every page, for example a navbar or a footer. If you want a custom layout for a specific page, then you must make the custom layout in that pages folder.

// The children in the root Layout are the pages/routes defined created in this app

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${montserrat.className} mx-32 my-12`}>
        {children}
      </body>
    </html>
  )
}
